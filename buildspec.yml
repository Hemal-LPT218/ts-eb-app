version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - echo "Installing dependencies"
    pre_build:
  commands:
      - echo Logging in to Amazon ECR...
      - aws ecr get-login-password --region ap-south-1 | docker login --username AWS --password-stdin 730335183789.dkr.ecr.ap-south-1.amazonaws.com
  build:
    commands:
      - echo "Building TypeScript project"
      - docker build -t demo-repository/docker-node-app .
  post_build:
    commands:
      - echo "Build completed"
      - docker tag demo-repository/docker-node-app:latest 730335183789.dkr.ecr.ap-south-1.amazonaws.com/demo-repository/docker-node-app:latest
      - docker push 730335183789.dkr.ecr.ap-south-1.amazonaws.com/demo-repository/docker-node-app:latest
      - echo "Push completed"
# artifacts:
#   files:
#     - dist/**/*
#     - scripts/**/*
#     - package.json
#     - appspec.yml
#     - ecosystem.config.js  # if using pm2
